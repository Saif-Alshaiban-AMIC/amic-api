# 1. Use the "pooler" endpoint for better stability in Spring
spring.datasource.url=jdbc:postgresql://ep-weathered-bar-al71ove8-pooler.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require

# 2. Username
spring.datasource.username=neondb_owner

# 3. Password (verified from your text)
spring.datasource.password=${DB_PASSWORD:npg_AiE03PFvdNIz}

# 4. Cleanup: Remove the manual dialect (Spring Boot 4 handles this)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect 

# 5. DDL Auto
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false


jwt.secret=${JWT_SECRET:dGhpcyBpcyBhIHNlY3VyZSBzZWNyZXQga2V5IGZvciBqd3Q}

# Mail — set MAIL_USERNAME and MAIL_PASSWORD as environment variables
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:salshaiban@alkhorayef.com}
spring.mail.password=${MAIL_PASSWORD:Ss@67890372}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.base-url=${APP_BASE_URL:http://localhost:4200}